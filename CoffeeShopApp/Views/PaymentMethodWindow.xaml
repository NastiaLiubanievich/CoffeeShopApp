<Window x:Class="CoffeeShopApp.PaymentMethodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оплата"
        Width="460"
        Height="330"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8F6F2">
    <Border Background="White"
            BorderBrush="#E7DED2"
            BorderThickness="1"
            CornerRadius="10"
            Padding="24,24,24,20"
            Margin="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Спосіб оплати"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#17120E"/>
                <TextBlock Text="Оберіть, як клієнт оплачує замовлення"
                           FontSize="15"
                           Foreground="#6E6256"
                           Margin="0,8,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,28,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Height="58"
                        Width="160"
                        Content="Картка"
                        FontSize="18"
                        VerticalContentAlignment="Center"
                        Click="CardButton_Click"/>
                <Button Grid.Column="2"
                        Height="58"
                        Width="160"
                        Content="Готівка"
                        FontSize="18"
                        VerticalContentAlignment="Center"
                        Style="{StaticResource GhostButtonStyle}"
                        Click="CashButton_Click"/>
            </Grid>

            <Button Grid.Row="2"
                    Content="Скасувати"
                    Height="42"
                    Width="130"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0"
                    Style="{StaticResource GhostButtonStyle}"
                    Click="CancelButton_Click"/>
        </Grid>
    </Border>
</Window>
